## 新增控制器：`JointPositionOscillationController`

### 设计思路

该控制器基于现有的 `JointPositionController` 框架，核心改动是支持**两个目标姿态之间的往复切换**：

1. **激活时**：从当前关节位置出发，先运动到目标 B
2. **到达目标 B 后**：以当前实际位置为新的起点，运动到目标 A
3. **到达目标 A 后**：再运动到目标 B，如此循环往复

每次运动都使用与 `JointPositionController` 相同的**五次多项式插值**（`s(t) = 6t⁵ - 15t⁴ + 10t³`）实现平滑轨迹，配合 PD 力矩控制。

### 新增/修改的文件

| 文件 | 操作 |
|------|------|
| [joint_position_oscillation_controller.h](file:///home/xusj/hil-serl-ros2/ros2_ws/src/serl_franka_controllers/include/serl_franka_controllers/joint_position_oscillation_controller.h) | 新增 - 头文件 |
| [joint_position_oscillation_controller.cpp](file:///home/xusj/hil-serl-ros2/ros2_ws/src/serl_franka_controllers/src/joint_position_oscillation_controller.cpp) | 新增 - 源文件 |
| [CMakeLists.txt](file:///home/xusj/hil-serl-ros2/ros2_ws/src/serl_franka_controllers/CMakeLists.txt) | 修改 - 添加新源文件 |
| [serl_franka_controllers_plugin.xml](file:///home/xusj/hil-serl-ros2/ros2_ws/src/serl_franka_controllers/serl_franka_controllers_plugin.xml) | 修改 - 注册新插件 |
| [serl_franka_controllers.yaml](file:///home/xusj/hil-serl-ros2/ros2_ws/src/serl_franka_controllers/config/serl_franka_controllers.yaml) | 修改 - 添加控制器配置 |
| [joint_oscillation.launch.py](file:///home/xusj/hil-serl-ros2/ros2_ws/src/serl_franka_controllers/launch/joint_oscillation.launch.py) | 新增 - launch 文件 |

### 关键参数

```yaml
joint_position_oscillation_controller:
  ros__parameters:
    arm_id: "fr3"
    motion_duration: 10.0                    # 单次运动持续时间（秒）
    target_joint_positions_a: [0.0, -0.785, 0.0, -2.356, 0.0, 1.571, 0.785]   # ready 位置
    target_joint_positions_b: [0.0, 0.168, 0.557, -2.193, -1.1723, 1.186, 0.110]  # STA 任务位置
    k_gains: [600.0, 600.0, 600.0, 600.0, 250.0, 150.0, 50.0]
    d_gains: [50.0, 50.0, 50.0, 50.0, 30.0, 25.0, 15.0]
```

### 使用方式

```bash
# 使用 launch 文件启动
ros2 launch serl_franka_controllers joint_oscillation.launch.py

# 或手动 spawn 控制器
ros2 control load_controller joint_position_oscillation_controller
ros2 control set_controller_state joint_position_oscillation_controller active
```

`motion_duration` 参数控制单次运动的时间（默认 10 秒），可以根据需要调整运动速度。