我整体上 希望实现一个 使用 franka 机械臂的 触觉传感器数据采集流程  其中 关于触觉传感器的部分 已经实现了 我主要需要实现使用机械臂进行的 标准化 自动化采集流程
目前的配置是这样 在机械臂的平行夹爪上 我安装了一对触觉传感器 我的人物是 将一系列不同的棍状物使用不同的夹取方式夹取 作为机器人工具使用的场景
我需要让 机械臂 夹着棍状物使用 棍状物在不同的表面上使用不同方式进行滑动

请基于/home/xusj/hil-serl-ros2/ros2_ws/src/serl_franka_controllers/src/joint_force_slide_controller.cpp，设计一个新的控制器 完成如下 采集数据流程：
1. 闭合末端夹爪
最多重复k=3次  每次判断是否夹取到物体 夹取到物体 或尝试夹取次数超过k=3 之后 切换到 phase 2
1.1 夹爪完全张开 
1.2 夹抓闭合（夹抓之间的距离最多闭合到最小直径d_min，或夹抓之间遇到阻力） 
1.3.检测夹抓是否夹住物体（确认夹抓闭合时 夹抓间距离在d_min - d_max之间 默认参数 d_min=0.5cm d_max=1cm）

2. Phase::TEACH（拖动示教）：控制器内部只计算纯重力补偿，关节刚度设为 0。
在这一阶段我会用手拖动机械臂，恰好让让平行夹爪抓住的棍状物贴在被滑动的表面上，标记为起始位置（x0, y0, z0）与起始姿态 q_0
在这期间
触发切换：控制器实时检测末端按钮。当你按下末端的“对勾（Enter）”键时，控制器瞬间记住当前的 q_0 和笛卡尔起始位置 Z（x0, y0, z0）作为 slide_base_z_，同时将状态切换到下一步。

到达示教终点位置后 点击键盘上的 1 键 确认示教位置
hil-serl-ros2/ros2_ws/src/serl_franka_controllers/scripts/keyboard_trigger_node.py
上述代码需要在独立终端中启动

3. Phase::CALIBRATE（自动最小二乘标定）：机械臂首先上升一定角度 到安全距离（太升末端夹爪0.15m）按照固定的标定方式晃动或运动到某些固定标记点，自动算好最新的 Mass 和 CoM（消除刚才新夹取姿态带来的误差）。
这里的最小二乘标定 你可以参考 /home/xusj/hil-serl-ros2/ros2_ws/src/FrankaEmikaPandaDynModel 中的方法

4. 完成上述任务后 进入数据采集循环

这里 dx dy dx_step dy_step F_0 在数据采集循环中 均不同（以函数参数形式传递 默认值0.01 cm 1N）
4.1 移动至Z（x0+dx, y0+dy, z0+0.05）
4.2 竖直下降至（x0+dx, y0+dy, z0）
4.3 在xy平面运动 (x0+dx, y0+dy, z0)-->(x0+dx+dx_step, y0+dy+dy_step, z0) 其中 z轴 使用 力控制（滑动过程中保持 F_0） xy轴使用位置控制

请完成整体流程设计与ros2 launch config文件的编写